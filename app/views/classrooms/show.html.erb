Classroom Info
<p>
<%= render partial: 'classroom', locals: {classroom: @classroom} %>
</p>

<%= @classroom.students.each do |student| %>
  <%= render 'students/student', locals: {student: @student}%>
<% end %>

 
